{
  "name": "flow-path",
  "version": "0.0.1",
  "peerDependencies": {
    "@angular/common": "^22.0.0",
    "@angular/core": "^22.0.0",
    "box-observer": "^0.0.1"
  },
  "dependencies": {
    "tslib": "^2.3.0"
  },
  "sideEffects": false,
  "scripts": {
    "build-wasm": "wasm-pack build src/lib/flow-path/path-finders/a-star-wasm --target web --release && node src/scripts/embed-wasm.js",
    "build": "npm run build-wasm && ng build flow-path"
  }
}
